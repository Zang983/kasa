export default function ContentBanner({image}) {
    return (
        <div>
            <h1>Chez vous, partout et ailleurs</h1>
            <img src={image} alt={"Bannière"} />
        </div>
    );
}